a(href='/todos/create') New

form(action='/todos/search', method='GET')
  input(type='text', name='q')
  button Search
ul
  each todo in todos
    li
      span=todo.action
      a(href='/todos/' + todo.id) Xóa
